apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  creationTimestamp: null
  labels:
    app: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: mysql
    spec:
      nodeSelector:
        worker_2: "true"  # Закрыты кавычки
      containers:
      - image: mysql:5.7
        name: mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: wordpress-config
              key: MYSQL_DATABASE
        volumeMounts:
        - name: myvol
          mountPath: /var/lib/mysql
      volumes:
      - name: myvol
        persistentVolumeClaim:
          claimName: mysql-pvc
