sudo bash -c 'apt update && apt upgrade -y && \
apt install nfs-common && \
mkdir -p /home/ubuntu/.kube/mnt/nfs_client && mount $(grep -oP '(?<=server: https://)[^:]*' /etc/kubernetes/kubelet.conf):/mnt/nfs_share /mnt/nfs_client && \
echo "finish" '
